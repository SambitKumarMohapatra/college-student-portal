spring.application.name=lms

# --- PostgreSQL (Render) ---
spring.datasource.url=jdbc:postgresql://dpg-d7kuq3pf9bms739nqfh0-a.oregon-postgres.render.com:5432/lms_db_3npv?sslmode=require
spring.datasource.username=lms_db_3npv_user
spring.datasource.password=yUi0wljDzWt5mPFCO3tNHtTHDhwVHK0t
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# --- JWT ---
app.jwt.secret=${JWT_SECRET:LandmineSoftLMSSecretKey2024VeryLongSecretKeyForSecurity!}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# --- JWT Refresh Token (7 days) ---
app.jwt.refresh-expiration-ms=${JWT_REFRESH_EXPIRATION_MS:604800000}

# --- Swagger ---
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

# --- Server ---
server.port=${PORT:8080}

# --- Email (Gmail SMTP) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:sk01mohapatra@gmail.com}
spring.mail.password=${MAIL_PASSWORD:znib hugw cqfq jsxu}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
